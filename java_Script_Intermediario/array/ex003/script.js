let fullName = "Isaque Rodrigues Alves"
console.log(fullName)

// Criar um array separados pelo espaço
console.log(fullName.split(" "))

// Cria um Array com as letras.
console.log(Array.from(fullName))