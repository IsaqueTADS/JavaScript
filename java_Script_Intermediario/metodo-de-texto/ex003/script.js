let text = "Estudar, Aprender, Praticar"

// Separar a String 
let separate = text.split(",")
console.log(separate)

let message = "não é possivel que um mero espaço de mierda pode ser usado pelo split para separar esse mega texro que estou digitando sorte do js eé que não sei se o lorem funciona auqi eu acho que não que pena jssss sortudo! "
console.log(message.split(" "))

// Unir a String

let joined = separate.join("")
console.log(joined)